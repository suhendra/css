<div class="row">
  <% Entry.feedback_option_list.each do |smiley| %>
    <div class="col-md-3">
      <%= link_to image_tag("svg_smileys/#{smiley[1]}.svg", alt: smiley[0]), send_feedback_entries_path(feedback: smiley[1]), remote: true %>
      <div class="text-center">
        <%= smiley[0] %>
      </div>
    </div>
  <% end %>
</div>
